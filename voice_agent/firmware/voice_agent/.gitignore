# ESP-IDF build output
build/
managed_components/

# sdkconfig contains WiFi credentials in plaintext — keep it local.
# sdkconfig.defaults (committed) seeds the values that aren't secret;
# the real WiFi SSID/password live only in your local sdkconfig.
sdkconfig
sdkconfig.old

# Generated by some IDE/idf.py versions
dependencies.lock
